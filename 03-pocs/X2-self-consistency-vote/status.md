# Status

**Doctrine:** Blocked

Self-consistency voting with gpt-4o-mini does NOT improve on a single gpt-4.1 call and is strictly dominated on the cost-quality Pareto frontier. Not a recommended routing strategy for this model pair and task distribution.

**Publishable as:** Negative result / teaching example. Documents why self-consistency fails when weak models hit a reasoning ceiling (systematic errors, not stochastic).
